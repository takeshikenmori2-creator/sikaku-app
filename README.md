# 海事代理士 筆記試験 演習アプリ

ランダムに4択問題を出し続けるだけの、個人学習用の静的アプリ。
`index.html` をブラウザで開けば動く（ビルド・サーバ不要）。

## 使い方

```
git clone <このリポジトリ>
cd sikaku-app
open index.html        # Linux なら xdg-open index.html
```

- 選択肢をクリック、またはキーの `1`〜`4` で解答
- 解答後は `Enter` または `Space` で次の問題へ
- ヘッダーに **正解数 / 出題数** と正答率を常時表示。連続正解が2以上になると連続数も出る
- スコアは**ページを開くたびにゼロから**。途中でリセットしたければヘッダーの「リセット」
- 「範囲」で科目を絞れる。1時限目／2時限目／3時限目／20点科目のプリセットあり（選択内容だけは次回も引き継ぐ）
- 間違えた問題は約1/3の確率でその場のセッション中に再出題される（範囲パネルでオフにできる）
- 選択肢の並び順は毎回シャッフルされる（同じくオフにできる）

## 中身

| ファイル | 役割 |
|---|---|
| `index.html` / `assets/` | アプリ本体。外部依存ゼロ |
| `data/subjects.json` | 筆記20科目の定義（配点・時限） |
| `data/src/*.json` | 科目ごとの問題ソース。**編集するのはここ** |
| `data/questions.js` / `.json` | `tools/build.py` が生成する配信用バンドル。直接編集しない |
| `tools/build.py` | `data/src/` をまとめて検証・生成する |
| `tools/fetch_official_pdfs.py` | 国交省サイトから過去問PDFを収集する |
| `tools/pdf_to_draft.py` | PDFから問題JSONの下書きを起こす |

問題を足したら必ず:

```
python3 tools/build.py
```

id重複・選択肢の重複・answerの範囲外などはビルドが弾く。

### 現在の収録数

全20科目 294問。配点20点の4科目（船舶法・船舶安全法・船員法・船舶職員及び小型船舶操縦者法）を厚めにしてある。

```
 14 憲法                    24 船舶法                12 港湾運送事業法
 16 民法                    22 船舶安全法            12 内航海運業法
 16 商法（第3編 海商）       12 トン数法              18 港則法
 10 国土交通省設置法         24 船員法                16 海上交通安全法
                            12 船員職業安定法        10 造船法
                            22 船舶職員法            14 海洋汚染防止法
                                                    10 国際航海船舶等保安法
                                                     8 領海等外国船舶航行法
                                                     8 船舶再資源化解体法
```

## 問題の出どころについて（重要）

**収録されているのは公式過去問そのものではなく、条文ベースで作成した演習問題である。**

このアプリを作ったセッションの実行環境では `mlit.go.jp` を含む外部ドメインへの通信が
ネットワークポリシーで遮断されており、公式PDFを取得できなかった。そのため問題は
試験範囲の各法令の条文・制度から作成してある。出題形式（4択）・科目構成・配点比率は
実際の筆記試験に合わせた。

条文の内容は正確を期しているが、一次資料は必ず
[e-Gov法令検索](https://laws.e-gov.go.jp/) で確認すること。

### 公式過去問を取り込む

配布元は国土交通省「海事代理士になるには」
<https://www.mlit.go.jp/about/file000049.html>
（平成29年〜令和7年の筆記試験模範解答、口述試験問題及び模範解答）。

```
python3 tools/fetch_official_pdfs.py --list        # 何が取れるか見る
python3 tools/fetch_official_pdfs.py --years 5     # 新しい5年分を downloads/ へ
python3 tools/pdf_to_draft.py downloads/2023_hikki_001579736.pdf --subject senpakuhou
```

`pdf_to_draft.py` は `data/draft/` に雛形を吐く。公式の過去問は記入式・記述式の設問を
含むので、`q` / `choices` / `answer` / `explain` を埋めて4択に組み直し、
`data/src/` に移して `tools/build.py` を実行する。

## 問題データの形式

`data/src/<科目id>.json`:

```json
{
  "subject": "senpakuhou",
  "questions": [
    {
      "id": "001",
      "q": "設問文",
      "choices": ["選択肢1", "選択肢2", "選択肢3", "選択肢4"],
      "answer": 2,
      "explain": "解説。選択肢の番号では参照しないこと（出題時にシャッフルされるため）",
      "ref": "船舶法1条"
    }
  ]
}
```

`answer` は0始まりの添字。最終的な問題idは `<subject>-<id>`。

## 試験の概要（参考）

- 筆記試験20科目・240点満点。配点20点が4科目、10点が16科目
- 合格基準は総得点の60%以上（受験者の平均正答率が60%を上回る場合はその平均以上）
- 口述試験は船舶法・船舶安全法・船員法・船舶職員及び小型船舶操縦者法の4科目

## ライセンス

個人学習用。
